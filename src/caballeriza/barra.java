package caballeriza;

public class barra {
    private char cbar, ccurrentPos;
    private int maxPos;

    public bar(char ccurrentPos, int maxPos){
        this.ccurrentPos = ccurrentPos;
        this.maxPos = maxPos;
        cbar = '-';
    }

    public void showBar(int currentPos){
        for (int i = 0; i < array.length; i++) {
            
        }
    }

}
